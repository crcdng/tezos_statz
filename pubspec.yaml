name: tezos_statz
description: Get a Tezos balance from the Blockwatch TzPro API.

publish_to: 'none' 
version: 1.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.0.11
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  just_audio: ^0.9.36
  qr_code_scanner: ^1.0.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3

flutter:
  uses-material-design: true

  assets:
    - assets/
  